/target
.DS_Store
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
/data
.env
# SSH keys / secrets — NEVER commit these (they grant access to the box + funds).
*.pem
*.key
id_rsa*
*_out
*_out.txt
*output*.txt
.worktrees/

# backtest runtime artifacts
bt-cache/
bt-results.json
