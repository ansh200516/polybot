//! M4 dashboard crate — populated in Tasks 3–5.
//! This crate is PURE PRESENTATION: it renders display-ready plain data
//! (Strings, f64 display-dollars) and emits commands; zero business logic,
//! zero µUSDC accounting.

pub mod render;
pub mod state;
pub mod ui;
